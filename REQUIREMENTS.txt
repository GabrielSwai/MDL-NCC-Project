# REQUIREMENTS.txt
# Bare-bones v0 uses only the Python standard library.
# Keep this file so environments are reproducible.

# Dev/test dependencies (optional but recommended):
pytest>=8.0
ruff>=0.5
mypy>=1.10